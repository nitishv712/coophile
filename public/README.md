# Static assets served from the site root.
# Kept tracked even when empty: git does not record empty directories, and the
# Dockerfile copies this path unconditionally, so a fresh clone without it
# would fail the image build.
